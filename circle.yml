machine:
  node:
    version: 6.8.1

general:
  branches:
    only:
      - master
      - /feature-.*/

dependencies:
  cache_directories:
    - "node_modules"

test:
  override:
    - npm run tests.all
  post:
    - if [[ -s npm-debug.log ]]; then cat npm-debug.log; fi

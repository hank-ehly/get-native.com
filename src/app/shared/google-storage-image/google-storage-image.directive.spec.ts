import { GoogleStorageImageDirective } from './google-storage-image.directive';

describe('GoogleStorageImageDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleStorageImageDirective();
    expect(directive).toBeTruthy();
  });
});
